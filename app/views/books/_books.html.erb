<h1>Books</h1>
<table>
  <tr>
    <td>Title</td>
    <td>Opinion</td>
  </tr>
  <% books.each do |book| %>
  <tr>
    <td><%= image_tag @user.get_user_icon(100, 100) %></td>
    <td><%= link_to book.title, book_path(book.id) %></td>
    <td><%= book.body %></td>
  <% end %>
  </tr>
</table>