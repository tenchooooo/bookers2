<h1>User inpo</h1>
<!--user紹介-->
<div>
  <%= image_tag @user.get_user_icon(100, 100) %><br>
  name <%= @user.name %><br>
  introduction <%= @user.user_introduction %>
</div>
<!--user投稿一覧-->
<h1>Books</h1>
<table>
  <tr>
    <td>Title</td>
    <td>Opinion</td>
  </tr>
  <% @books.each do |book| %>
  <tr>
        <td><%= link_to book.title, book_path(book.id) %></td>
        <td><%= book.body %></td>
      </p>
    </div>
  <% end %>
  </tr>
</table>
